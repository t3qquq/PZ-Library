module Base
{
    template vehicle Radio
    {
        part Radio
        {
            mechanicArea = Engine,
            area = Engine,
            itemType = Base.RadioBlack;Base.RadioRed;Base.RadioMakeShift,
            specificItem = false,
            durability = 2,
            table install
            {
                items
                {
                    1
                    {
                        tags = base:screwdriver,
                        count = 1,
                        keep = true,
                        equip = primary,
                    }
                }
                time = 100,
                test = Vehicles.InstallTest.Default,
                complete = Vehicles.InstallComplete.Radio,
            }
            table uninstall
            {
                items
                {
                    1
                    {
                        tags = base:screwdriver,
                        count = 1,
                        keep = true,
                        equip = primary,
                    }
                }
                time = 100,
                test = Vehicles.UninstallTest.Default,
            }
            lua
            {
                create = Vehicles.Create.Radio,
                update = Vehicles.Update.Radio,
            }
        }
    }
}
